<h1>See what our users are reading:</h1>

<% if flash[:alert] %>
        <p><%= flash[:alert] %><p>
<% end %>

<ul>
<% @books.each do |book| %>
    <li>
        <%= link_to book.title, book_path(book) %>
    </li>
<% end %>
</ul>
