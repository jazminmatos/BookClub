<nav class="navbar navbar-light" style="background-color: #e3f2fd;" >
  <ul class="nav">

    <li class="nav-item">
      <div class="nav-link active"><%= link_to "Home", '/' %></div>
    </li>

    <li class="nav-item">
      <div class="nav-link active"><%= link_to "About", '/about' %></div>
    </li>

    <li class="nav-item">
      <div class="nav-link active"><%= link_to "BookClubs", clubs_path %></div>
    </li>

    <% if user_signed_in? %>
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <div class="dropdown-item"><%= link_to "Profile", user_path(current_user) %></div>
            <div class="dropdown-item"><%= link_to "Settings", edit_user_registration_path %></div>
            <div class="dropdown-item"><%= link_to "Sign Out", destroy_user_session_path, :method => :delete  %></div>
          </div>
        </li>
    <% end %>

    <% if !user_signed_in? %>
      <li class="nav-item">
        <div class="nav-link active"><%= link_to "Sign In", new_user_session_path %></div>
      </li>
    <% end %>
    
  </ul>
</nav>